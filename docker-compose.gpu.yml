services:
  agent:
    gpus: all
    environment:
      STT_DEVICE: cuda
      STT_MODEL: ${STT_MODEL:-turbo}
      STT_COMPUTE_TYPE_GPU: ${STT_COMPUTE_TYPE_GPU:-float16}
      DEBUG_SAVE_WAV: ${DEBUG_SAVE_WAV:-false}
