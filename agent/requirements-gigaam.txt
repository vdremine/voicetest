# Optional GigaAM STT backend (Sber, MIT) — installed as a NON-FATAL Docker layer.
# Activated at runtime with STT_BACKEND=gigaam. Falls back to faster-whisper if
# unavailable (see build_stt_service).
# CPU onnxruntime (NOT [gpu]): the container ships CUDA 12 libs, while a recent
# onnxruntime-gpu wants CUDA 13 (libcudart.so.13) -> import fails. CPU avoids the
# mismatch and reuses the onnxruntime already present (used by Silero VAD).
# GigaAM is a small 0.6B RNN-T; CPU is workable for telephony.
onnx-asr[cpu,hub]
