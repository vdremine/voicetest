# Optional TTS providers — installed as a NON-FATAL Docker layer.
# Activated at runtime with TTS_PROVIDER=omnivoice | piper. If install fails
# (e.g. a torch conflict), the image still builds and the agent falls back to
# Silero TTS automatically (see build_tts_service in voice_loop.py).
omnivoice
soundfile
piper-tts
